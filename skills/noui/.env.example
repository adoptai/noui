# NoUI → Cloud Tabby (agent client/secret, agent_token mode)
# Fill in the three values below, then save. This file is gitignored.

TABBY_API_URL=
TABBY_CLIENT_ID=
TABBY_CLIENT_SECRET=
TABBY_ADMIN_TOKEN=

# Optional — only needed for register/promote (Pillar 3 Activate), which the
# agent token cannot do (POST /apps + /admin/profiles require Admin/Operator):
# TABBY_ADMIN_TOKEN=REPLACE-with-admin-token
